{
  "name": "ng-15-20-compat",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "build": "nx run-many -t build",
    "build:host": "nx build host-v15",
    "build:shared": "nx build shared",
    "publish:shared": "cd dist/libs/shared && pnpm publish --no-git-checks",
    "serve:host": "nx serve host-v15"
  },
  "dependencies": {
    "zone.js": "~0.12.0"
  },
  "devDependencies": {
    "@nx/js": "19.8.0",
    "nx": "19.8.0",
    "typescript": "~4.9.0"
  }
}
